I want to build a mobile-friendly web app that mimics the core functionality of the “Push-Ups” app in the iOS App Store (black background, large green circle for reps), but I want to significantly expand and improve its functionality. Please help me build the first working version.

⸻

🎯 Core Functionality:
	1.	The app should have tappable circular buttons for 4 exercises:
	•	Pushups
	•	Bodyweight Squats
	•	Sit-ups
	•	Pull-ups (or Bar Hang seconds)
	2.	Tapping a button should increase today’s count by 1 for that exercise.
	3.	Each exercise should have its own labeled green circular counter (similar in design to the Push-Ups app) that increases as I tap.
	4.	Include an UNDO button below each circle to subtract 1 from the current day’s count if tapped too many times.
	5.	Show the current date at the top, and automatically track/log the reps for that date only.
	6.	Store the log of reps locally in the browser using localStorage. This should persist even if the page is refreshed.

Design Goals:
	•	Keep the UI simple and clean like the original Push-Ups app.
	•	Use a dark background with bright circular counters.
	•	Ensure it looks good on mobile (responsive layout).
	•	Circles should scale slightly or visually “bounce” when tapped (optional, but nice UX).
	•	Show a confirmation animation or flash when a tap registers (optional).

⸻

🧠 Technical Details:
	•	Use HTML/CSS/JavaScript only (no frameworks unless needed for UI polish).
	•	Store logs in localStorage using keys like:
log = { 'Jul 10, 2025': { pushups: 160, squats: 100, situps: 0, pullups: 0 } }
	•	On app load, pull today’s entry from storage, or create a new one.
	•	On every tap or undo, immediately update both the UI and localStorage.

⸻

🔄 Future Features (optional, for later phases):
	•	Allow users to edit previous day’s counts if they forgot to log something.
	•	Add goal tracking (e.g., “200 reps/day goal met!”)
	•	Allow changing exercises or number of visible trackers.
	•	Export log as CSV.
	•	Save to cloud with Firebase/Supabase (for future login/syncing).

⸻

👋 What I Want From You (Replit AI):

Start by helping me build the core HTML, CSS, and JavaScript for this:
	•	Mobile-friendly layout
	•	4 tappable circular counters
	•	Undo buttons
	•	Date tracking
	•	localStorage for saving logs
	•	Scrollable daily log list

Please explain each step briefly as we go, and let me know how to customize it later (e.g., to add more exercises or change the style). Let’s build it together step by step.